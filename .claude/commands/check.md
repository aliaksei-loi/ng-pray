Run all checks to verify the project is healthy.

Steps (run in parallel where possible):
1. `pnpm lint` — check for lint errors
2. `pnpm build` — verify TypeScript compilation and Next.js build

Report any errors found. If everything passes, confirm the project is clean.
