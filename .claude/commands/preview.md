Open the app in a Playwright browser for visual verification.

Steps:
1. Check if the dev server is running on port 3000. If not, start it with `pnpm dev` in the background
2. Use the Playwright MCP to navigate to http://localhost:3000
3. Take a screenshot and show it
4. If $ARGUMENTS contains a path (e.g. "/dashboard"), navigate there instead
