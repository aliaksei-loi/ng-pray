Push the current Prisma schema to the database without creating a migration (for prototyping).

Steps:
1. Run `pnpm exec prisma db push`
2. Run `pnpm exec prisma generate`
3. Report success or errors
