Add a shadcn/ui component to the project.

The component name is provided as $ARGUMENTS. If no name is provided, ask the user which component they want.

Steps:
1. Run `pnpm exec shadcn add $ARGUMENTS`
2. Report which files were created/updated
